# R&C Company (TVD) — 작업물 리포트

넥슨 게임잼 '재밌넥' 2차 과제전형 제출물 보고서
개발 기간: 2026.07.03(금) ~ 07.06(월) 15:00 / 팀 구성: 1인 (전 포지션 AI 협업)

---

## 1. 결과물에 대한 설명

### 개요

- **타이틀**: R&C Company (개발 코드명: TVD — Top-View Defense)
- **장르**: 탑뷰 하이브리드 디펜스 — 플레이어 직접 조작(이동/자동공격/스킬) + 타워 설치를 결합
- **엔진**: Unity 6 (URP 2D), 신규 Input System, TextMeshPro
- **빌드**: Windows Standalone / WebGL(Web)

플레이어는 R&C 컴퍼니 소속 파일럿이 되어, 오퍼레이터 '카시아'의 지원을 받으며 단일 경로를 따라 몰려오는 적 웨이브로부터 전초기지를 방어한다. 직접 움직이며 적을 처치해 골드와 경험치를 얻고, 골드로 그리드 슬롯에 타워를 설치하며, 레벨업 시 무작위 3장 중 1장을 고르는 업그레이드 카드(뱀서라이크 스타일)로 매 판 다른 빌드를 만들어 나간다. 거점 또는 플레이어의 체력이 0이 되면 게임 오버 — 도달 웨이브/처치 수/획득 골드/생존 시간이 결과 화면에 표시된다.

### 게임 루프

1. 웨이브 시작 → 적 스폰 (예산 기반 절차적 생성)
2. 플레이어 이동/자동 공격으로 적 처치 → 골드·경험치 획득
3. 레벨업 시 일시정지 + 카드 3장 중 1장 선택 (플레이어 강화 / 타워 강화 / 슬롯 확장 / 신규 타워 해금 등)
4. 처치 보상 골드로 타워 설치 (필요 시 페널티를 감수하고 철거·재배치)
5. 웨이브 클리어 → 10초 빌드 페이즈 (HUD 카운트다운 — 실질적인 타워 설치/재배치 타이밍 창)
6. 반복하며 난이도 상승 — 웨이브가 오를수록 적 수량과 개체당 체력이 함께 스케일링

### 기획 의도 — 모든 결정의 공통 기준

모든 설계 결정은 **"1인 개발, 3일 제작 기간, AI-Native 게임잼 평가 기준(AI 활용도 + 종합적 게임 제작 역량)"**이라는 제약 조건을 기준으로 내렸다.

- **장르 — 하이브리드(직접 조작 + 타워)**: 순수 타워 디펜스는 배치/업그레이드 UI 비중이 높고 재미가 거의 전적으로 밸런싱에 종속되어 3일 안에 완성도를 내기 어렵다고 판단했다. 하이브리드는 플레이어 조작이 즉각적인 재미를 주고, 타워는 적은 종류로도 전략적 깊이를 줄 수 있어 구현량을 양쪽으로 분산시켜 리스크를 낮췄다.
- **성장 — 랜덤 업그레이드 카드(뱀서 스타일 3택)**: 정교한 타워 트리를 처음부터 설계하는 대신, 카드 풀을 넉넉하게 만들어두면 조합이 자연스럽게 다양해지는 구조로 밸런싱 부담을 줄였다. 동시에 매 판 다른 조합이 나오는 리플레이성을 확보해 짧은 시연 영상에서도 "성장하는 느낌"을 보여줄 수 있게 했다.
- **카드 풀 비중 — 타워/거점 관련 73%**: 랜덤 성장을 도입하더라도 게임이 "디펜스"라는 정체성을 잃지 않도록 타워/거점 관련 카드 비중을 의도적으로 높게 설계했다. 플레이어 강화만 계속 나오면 사실상 뱀서라이크가 되어버리기 때문.
- **절차적 생성 — 웨이브만 적용, 맵은 미도입**: 맵 절차적 생성은 경로탐색·디버깅 비용이 크고 매판 타워를 다시 배치해야 하는 피로도가 효과 대비 크다고 판단해 제외했다. 대신 구현 비용이 낮고 효과가 확실한 웨이브 구성 절차적 생성에 리소스를 집중해, AI를 활용한 밸런싱 설계를 보여줄 포인트로 삼았다.
- **맵 구조 — 단일 경로**: 오픈 필드는 적 경로탐색(NavMesh/A*)과 방어 동선 설계가 복잡해져 범위를 초과한다고 판단. 단일 경로는 웨이포인트만으로 구현 가능하고, 경로 모양만 바꾸어도 충분히 다른 느낌을 줄 수 있다.
- **플레이어 체력 — 보유하되, 어그로 시스템은 배제**: 플레이어가 무피해라면 "이동 가능한 포탑"이 되어버려 직접 조작의 액션감이 생기지 않는다고 판단해 체력을 도입했다. 다만 적이 플레이어를 추적/타겟팅하는 어그로 AI는 구현 비용만 늘리므로 제외 — 적은 경로만 따라가다가 플레이어와 가까워지면 접촉 데미지를 입히는 수동적 방식(거리 판정)으로, 최소 비용으로 위험 요소를 확보했다.

### 핵심 시스템

**타워 — 3분류 × 총 18종**
직접 딜러(공격), 범위 버프(스킬), 글로벌 버프(파워)로 역할을 분리해 서로 다른 배치 전략이 자연스럽게 발생하도록 설계했다.

- **공격 타워**: 사거리 내 적을 자동 공격. 기본형 외에 스나이퍼(사거리↑/공속↓), 속사포(반대), 폭발탄(범위 피해), 맹독(DoT), 관통 사격(직선 관통) 등 6종.
- **스킬 타워**: 자기 사거리 내의 아군 공격 타워를 상시 강화(공격력/공속). 사거리를 벗어나면 버프가 자동 소실되므로 **배치 클러스터링이 전략의 핵심**이 된다. 버프형 외에 적을 대상으로 하는 디버프형(빙결 오라 — 감속, 취약 오라 — 받는 피해 증가)까지 6종.
- **파워 타워**: 사거리 개념 없이 건설 즉시 전역 버프. 조기 건설이 유리한 타이밍 전략 축. 전역 공격력/공속, 거점 재생, 골드 채굴 등 6종.
- **슬롯 제한**: 종류별 설치 수 제한(기본 공격3/스킬1/파워2)을 둬 무제한 도배를 막고, 슬롯 확장 자체를 카드 보상(빌드 전략)으로 만들었다. 특히 스킬 타워는 버프형이라 수가 많아지면 밸런스가 쉽게 무너져 기본값을 가장 낮게 잡았다.
- **철거 페널티**: 철거는 환불이 아니라 건설비의 75%를 **추가로** 지불 — 건설/철거 반복으로 슬롯 제한을 우회하는 것을 막되, 이 비율 자체를 카드로 할인받을 수 있게 해 재배치 빌드도 선택지로 열어뒀다.

**적 — 베이스 3종 + 웨이브 스케일링**
적 종류를 늘릴수록 그래픽 작업량과 웨이브 생성 규칙 분기가 함께 늘어나므로, 역할이 뚜렷한 3종으로 압축했다.

- **일반형**(표준 체력/속도) / **돌진형**(체력↓ 속도↑ — 플레이어의 반응속도를 시험) / **탱커형**(체력↑ 속도↓ — 화력 집중을 시험)
- 로직/프리팹은 전 종류가 1개를 공유하고 스탯·스프라이트만 데이터(SO)로 주입 — 종류 추가가 데이터 작업만으로 끝나는 구조("툴백 라인"을 아예 기본 구조로 채택).
- 웨이브가 오를수록 개체당 체력에 배율(웨이브당 +10%)이 곱해진다. 수량(예산)만 늘리면 타워 DPS가 누적될수록 상대적으로 쉬워지는 역설이 있어, 개체 강도도 함께 올려 "무한 디펜스"의 압박감을 유지했다.

**웨이브 절차적 생성 — 예산(Budget) 방식**
수동 밸런싱 대신 숫자 몇 개(기본예산, 성장계수, 적 코스트)만 조정하면 난이도 곡선이 통째로 따라오는 구조를 택했다 — 구현과 밸런싱 양쪽의 시간 비용을 모두 줄이는 선택.

- `예산(n) = 기본예산 + 성장계수 × n` (예: 10+2n → 웨이브1: 12, 웨이브10: 30)
- 적마다 코스트(일반 1 / 돌진 1.5 / 탱커 2.5)와 최소 등장 웨이브(1/3/5)를 부여 — 웨이브 번호로 필터링된 후보군에서 예산 소진까지 추첨해 스폰 큐를 구성.
- 스폰 간격도 웨이브에 비례해 단축: `max(0.5초, 2.0초 − 0.05×n)`.
- **난수 독립성·재현성 보장**: 웨이브 생성 전용으로 시드 고정된 `System.Random`을 사용해 전역 `UnityEngine.Random`과 분리 — 같은 시드면 같은 웨이브 구성이 재현되고, 다른 시스템의 난수 호출이 웨이브 결과에 영향을 주지 않는다.
- 보스 웨이브 분기(5의 배수 웨이브)는 구조만 마련 — 보스 유닛 자체는 GDD에서 "여유시" 항목이라 스코프에서 제외하고, 확장 지점(필드)만 남겨뒀다.

**업그레이드 카드 — GDD 확정 15장 → 최종 27장**
레벨업 시 일시정지 후 3장 제시, 1장 선택. 플레이어 강화(공격력/이속/스킬 쿨다운) / 타워 강화(데미지/사거리/버프량) / 슬롯 확장(종류별 +1) / 거점(최대체력/재생) / 신규 타워 해금으로 구성.

- 개발 중 타워 로스터를 18종으로 확장하면서 해금 카드 12장이 추가되어 GDD 확정 15장에서 27장으로 늘었다.
- 신규 타워 해금은 기존 기본형의 **교체가 아니라 추가** — 같은 종류의 기본형/특수형이 슬롯 한도를 나눠 쓰며 공존해, 상황에 따라 골라 짓는 선택지가 된다.
- 이미 해금된 타워의 해금 카드는 추첨에서 자동 제외(`IsAvailable` 필터) — 무의미한 선택지가 나오지 않게 함.
- 레벨업 필요 경험치는 선형이 아닌 **제곱 스케일링**(`base + growth × Level²`) — 타워 DPS가 누적될수록 처치 속도(=초당 경험치)가 빨라져 후반 레벨업이 오히려 쉬워지는 역설을 막기 위함.

**효과 중첩 규칙 — "이상한 빌드를 짜도 게임이 터지지 않게"**
여러 효과가 같은 대상에 겹칠 때의 규칙을 명시적으로 구분해 구현했다.

- **적 상태이상(감속/취약/맹독)은 중첩 없음, 덮어쓰기만**: 같은 적에게 몇 대를 겹쳐 걸어도 배율이 곱해지지 않고 지속시간만 갱신 — 속도가 0으로 수렴하거나 피해가 폭주하는 경우가 구조적으로 불가능.
- **타워 버프는 의도적으로 중첩**: 스킬 타워 오라는 겹친 개수만큼 곱연산(+20% 3개 = 1.2³ ≈ 1.73배), 파워 타워는 합연산 누적 — 배치 전략의 핵심이므로 의도된 동작.
- 카드 스탯 배율도 중복 획득 시 곱연산 누적(로그라이크 성장 의도). 단 "오라 증폭" 카드는 배율 전체가 아니라 보너스 부분만 강화(1.2→1.25배)하도록 설계해 극단적 폭주를 방지.

**플레이어 — 오버드라이브 스킬**
기본 공격은 사거리 내 최근접 적 자동 발동(쿨다운 기반). 스킬은 1회성 즉발 AoE 대신 **"오버드라이브 모드"** — 0.3초 간격 4연타 광역 타격 + 지속시간 동안 이동속도 1.5배. 매 타격이 적 피격 틴트를 연쇄로 터뜨려 별도 이펙트 에셋 없이도 화면에서 강한 타격감을 만든다.

**메타/연출 시스템**

- **오퍼레이터 '카시아'**: 게임 개시/스킬 사용/거점·플레이어 피격(저체력 특수 대사 포함)/건설 실패 2종/사망 2종 — 8개 상황에서 초상화 전환 + 랜덤 대사 말풍선. 캐릭터성 있는 대사로 상태 정보(골드 부족, 슬롯 부족, 거점 위험)를 전달하는 진단 UI를 겸한다.
- **튜토리얼**: 게임 시작 시 페이지형 튜토리얼(주제/삽화/설명) 자동 표시, 읽는 동안 게임 일시정지.
- **미션 결과 화면**: 도달 웨이브/처치 수/획득 골드/생존 시간 + Retry(즉시 재시작)/Title 버튼.
- **타이틀 씬**: "PRESS ANY BUTTON" 타이틀 → 메인 메뉴 전환 연출, 캐릭터 실루엣 라인아트 안에 인게임 플레이 영상 재생(VideoPlayer + RenderTexture), 설정 화면(해상도/화면모드/VSync/볼륨 3종 슬라이더).
- **사운드**: BGM 랜덤 플레이리스트(직전 곡 연속 재생 방지), AudioMixer 기반 Master/BGM/SFX 볼륨(PlayerPrefs 영속), 효과음 풀링 재생, 타워 공격음은 플레이어 주변에서만 들리는 거리 게이팅(타워가 많아져도 소리가 겹쳐 터지지 않게).

### 아트 디렉션

- **스타일**: 플랫 실루엣 + 기하학적 포인트 컬러 (별/컴퍼스 형태 모티프). 디테일 드로잉 없이 형태감만으로 완성도를 내는 방식이라 1인·3일 개발 제약에 부합 — 레퍼런스 이미지를 기준으로 GDD에 방향을 먼저 확정하고, 모든 그래픽 생성을 이 기준으로 디렉팅했다.
- **색 체계**: 모노톤 + 단일 포인트 컬러(블루/시안) 기본. 타워 3분류를 색상(공격=레드/스킬=시안/파워=옐로)으로 구분해 HUD·빌드 메뉴·카드 프레임까지 일관되게 적용 — 색만 보고도 종류를 식별할 수 있게 했다.
- **타이포그래피**: 굵은 산세리프 대문자 타이틀은 타이틀/로고 화면에만 제한적으로 사용.
- 제작비 대비 효과가 낮다고 판단한 방향(실사 항공샷 배경 합성, 망가 잉크 일러스트)은 레퍼런스 단계에서 명시적으로 기각.

### 스코프 관리 — 의도적으로 하지 않은 것

3일 제약에서 완성도를 지키기 위해, "안 할 것"을 기획 단계에서 명시하고 문서에 남겼다.

- **맵 절차적 생성** (피로도 대비 효과 미흡), **적 어그로/추적 AI** (수동적 접촉 데미지로 대체), **수식어(정예/무리) 시스템과 보스 유닛** (GDD "여유시" — 확장 지점만 코드에 마련), **메타 프로그레션(로비 씬/영구 해금)** (세이브/로비 UI 비용 대비 3분 시연에서 효과를 못 보여줌 — 런 내 카드 해금으로 "타워 다양성" 요구를 대체 충족), **오브젝트 풀링의 전면 도입** (실측 기준 저빈도 이펙트는 풀링하지 않고, 고빈도인 이동 잔상·공격 플래시만 선별 풀링).

---

## 2. 제작과정

### 사전 기획 (Day 0~1 오전) — GDD 공동 작성

Claude(데스크탑 앱)와 Notion MCP 연동으로 GDD를 대화하며 작성했다. "1인·3일·AI-Native 평가 기준"이라는 제약을 명시하고 장르/성장 방식/절차적 생성 범위/맵 구조/적 구조/카드 목록/아트 방향까지 모든 축을 토론으로 결정 — 각 결정에는 반드시 "왜"(기획 의도)를 함께 기록해, §1의 Design Rationale이 그 산출물이다. 오픈 이슈는 체크리스트로 관리해 Day 1 시작 전에 전부 확정 처리했다.

특히 이전 개인 프로젝트(Project Vertex, 슬레이 더 스파이어 모작)의 실제 코드 구조를 AI와 함께 분석해 두 가지 핵심 설계를 기술 노트로 GDD에 미리 확정했다:

- **매니저 원칙**: 매니저는 "게임 흐름 단계"당 1개(Game/Map/Wave/Card — 4개)만 두고, "오브젝트 타입"별 매니저(EnemyManager/TowerManager)는 만들지 않는다. 개별 오브젝트가 스스로 상태를 들고 이벤트로 알리며, 매니저는 리스트 순회와 흐름 조율만 한다.
- **효과 훅 시스템**: 타워 특수능력은 `ITowerEffect` 인터페이스(계약) + `TowerEffectBase : ScriptableObject`(편의 기본 구현)로 분리하고, 훅 시점을 타워 성격별로 나눔(공격=OnTick 반복 / 스킬=아군 사거리 출입 / 파워=건설 시 1회). 스킬 타워 버프는 스탯 직접 가감이 아니라 **파이프라인 조회**(`ITowerAura.ModifyOutgoingDamage`) — 공격 타워가 자기에게 걸린 버프 목록만 들고 있다가 데미지 계산 순간에만 조회해, 사거리 이탈 시 자동 소실되고 중첩도 안전한 구조.

### 개발 협업 규칙 — AI에게 먼저 규칙을 계약시킴

구현 착수 전, 코딩 AI(Claude Code)에게 다음 작업 규칙을 초기 프롬프트로 명시했다:

- 4계층 아키텍처(데이터 컨테이너 → 데이터 기반 SO → 렌더러 프리팹 → 매니저)를 따를 것
- 기획이 불명확하면 **개발을 중단하고 질문**할 것 (임의 확정 금지)
- 한 번에 한 모듈 이하의 작은 단위로만 진행할 것
- 구조적 결정과 이유를 자체 문서(`ARCHITECTURE.md`)에 기록하고, 진행 상황을 Notion 체크리스트에 남길 것
- 타워 설치는 그리드(Tilemap), 적/플레이어 이동은 그리드와 무관한 자유 좌표일 것
- 웨이브 절차적 생성은 난수 독립성과 재현성을 보장할 것
- 에디터 자동화 컴포넌트를 무리하게 만들기보다, 인스펙터 수작업이 필요한 부분은 사람에게 지시할 것
- 매 출력 마지막에 사람이 수행해야 할 태스크를 명시할 것

이 규칙 덕에 "AI가 한 번에 다 만들었다가 어디서 깨졌는지 모르는" 상황 없이, 매 단위마다 **사람이 씬 세팅 → Play 테스트 → 스크린샷/증상 보고 → AI가 원인 분석·수정**하는 검증 루프가 유지되었다.

### Day 1 (7/3) — 데이터 계층과 코어 루프

데이터 컨테이너(TowerData/EnemyData/PlayerData) → 효과 훅 레이어 → Definition SO → 매니저(Map/Wave) 순으로 아래에서 위로 쌓았다. 개체별 처리 방식은 개체 수 차이에 따라 의도적으로 다르게 갔다:

- **적 = 순수 C# 인스턴스** (MonoBehaviour 아님): 실시간 웨이브라 동시 개체 수가 많아 개체별 `Update()` 오버헤드가 치명적 — `EnemyInstance`가 스스로 `Tick()`/`TakeDamage()`를 갖고 이벤트(`Damaged`/`Died`/`ReachedGoal`)로 알리며, WaveManager가 리스트를 순회. 시각 표현은 로직 없는 `EnemyView`(MonoBehaviour)가 위치만 반영.
- **타워 = 일반 MonoBehaviour + Collider2D 트리거**: 슬롯 제한으로 개체 수가 원래 적어 순수 C#화가 불필요 — 아군 타워 출입(스킬 타워)과 적 진입(공격 타워)을 같은 물리 트리거로 감지.

Day 1 종료 시점에 "적 스폰 → 웨이포인트 경로 추종 → 타워/플레이어 공격 → 피해/처치 보상"의 코어 루프가 Play 모드에서 검증 완료. 중간에 파일당 클래스 1개 원칙 위반(SO/MonoBehaviour를 파일명과 다른 이름으로 한 파일에 몰아둔 것 — 이름 변경 시 에셋 참조가 깨지는 Unity 특유의 리스크)을 사람이 지적해 전면 분리 리팩토링을 거쳤다.

### Day 2 (7/4~5) — 빌드 시스템, 절차적 웨이브, 카드

- 그리드 클릭 타워 설치(커서 추적 반투명 프리뷰 + 설치 가능 여부 색상), 예산 기반 웨이브 절차적 생성(고정 간격 스폰 베타를 GDD 규칙대로 확장), 레벨/경험치 + 카드 시스템(프레임워크 → 15장 구체 효과 → 선택 UI 순), HUD를 구현.
- 카드 효과를 실제로 붙이는 과정에서 기존 시스템의 간극(사거리 카드를 먹어도 물리 콜라이더가 안 늘어남, 파워 타워 강화 카드가 이미 지은 타워에 소급 안 됨 등)이 드러나 함께 확장 — "카드가 실제로 작동하려면 필요한 것"이 무엇인지 구현으로 검증하며 진행했다.
- Tower/Enemy/Card 모두 "로스터 SO" 패턴으로 통일해 매니저들이 개별 배열 대신 공유 에셋 하나를 참조하도록 정리했고, 적/타워 프리팹을 각 1개로 통합(외형·스탯은 전부 Definition 데이터로 주입)해 종류 추가 비용을 데이터 작업만으로 낮췄다.

### Day 3 (7/5~6) — 확장과 폴리싱

- **타워 로스터 18종 확장**: "새 코드가 정말 필요한 종류"를 먼저 분석 — 18종 중 7종은 기존 이펙트의 수치 변형만으로(같은 이펙트 SO, 다른 Definition 데이터), 5종만 신규 이펙트 클래스(폭발탄/맹독/취약 오라/전역 가속/골드 채굴)로 커버. 데이터 기반 설계가 실제로 확장 비용을 낮추는지 검증된 지점.
- **시각/청각 피드백 일괄 작업**: 공격 라인 플래시(풀링), 이동 잔상(풀링), 피격 틴트(적/플레이어), 적 체력바(만피 시 자동 숨김), 사거리 인디케이터, 스킬 재설계(오버드라이브 모드), 사운드 매니저.
- **메타 화면**: 오퍼레이터 대사, 튜토리얼, 미션 결과 화면(Retry 시 세션 캐시 초기화 포함 — §4-2), 타이틀 씬 통합(설정 화면의 볼륨/해상도 시스템 연결), 타이틀 실루엣 영상 연출.
- 밸런스 보정 2건을 플레이테스트에서 도출: 레벨업 경험치 선형→제곱 전환, 웨이브별 적 체력 스케일링 추가 — 둘 다 "성장이 누적될수록 고정 콘텐츠가 상대적으로 쉬워지는" 같은 근본 원인에 대한 대응.
- Web(WebGL) 빌드로 마무리.

### 문서화

모든 구조적 결정과 그 이유(왜 이 방식을 택했고 무엇을 의도적으로 안 했는지)를 `Assets/Docs/ARCHITECTURE.md`에 개발 내내 누적 기록했고(약 1,100줄), 진행 상황은 Notion 체크리스트에 미러링했다. 개발 중 확정된 기획(빌드 페이즈 10초, 효과 중첩 규칙, 오퍼레이터 대사 스크립트 등)은 GDD에 역반영해 기획서와 구현이 어긋나지 않게 유지했다. 이 문서들이 있어 세션이 바뀌어도(AI 컨텍스트가 초기화되어도) 일관된 원칙으로 개발을 이어갈 수 있었다.

---

## 3. AI 활용 내역

| 용도 | 도구 | 활용 방식 |
|---|---|---|
| 기획 (GDD) | **Claude** (데스크탑 앱 + Notion MCP) | 장르/시스템/아키텍처를 토론으로 결정하고 합의 내용을 AI가 직접 Notion GDD에 반영. 이전 프로젝트 코드 분석 기반 기술 노트 작성 |
| 게임플레이 코드 | **Claude Code** (VS Code 확장, Sonnet 5 → Fable 5) | 4계층 아키텍처 기반 전체 게임플레이 구현, 버그 원인 분석, ARCHITECTURE.md 결정 로그 작성, 라이선스 등 실시간 웹 리서치 |
| UI/스프라이트 | **GPT** (이미지 생성) | GDD의 아트 디렉팅(플랫 실루엣 + 기하학 포인트 컬러)과 레퍼런스 이미지를 기준으로 대화하며 반복 디렉팅 — 타이틀/HUD/카드 프레임/타워·적 스프라이트 등 그래픽 대부분 |
| 타이틀 씬 코드 | **Codex** (VS Code 확장) | 타이틀/메인 메뉴/설정 화면의 연출·전환 코드 초기 구현 (이후 Claude Code가 사운드/볼륨/해상도 시스템과 통합) |
| 오퍼레이터 일러스트 | **NovelAI** | 캐릭터(카시아) 초상화 생성 — 프롬프트 전문은 Notion '제작과정' 페이지에 기록 |
| 효과음 | **ElevenLabs** | 전 효과음 AI 생성 (예: 타워 설치음 프롬프트 `placing sound effect game sfx`) |
| BGM | Pixabay Music (비-AI, 무료 라이선스) | 당초 후보였던 NCS가 게임 임베드에 별도 상업 라이선스를 요구함을 Claude의 실시간 약관 조회로 확인 후, 게임 사용이 명시적으로 허용된 소스로 교체 |

### AI 협업 방식의 특징

1. **역할 분담**: 기획 토론(Claude 데스크탑) / 게임플레이 코드(Claude Code) / 아트(GPT·NovelAI) / 타이틀 연출(Codex) / 사운드(ElevenLabs)로 도구를 나누고, 사람은 최종 결정·인스펙터 작업·플레이 검증·통합을 담당.
2. **"질문 우선" 원칙**: 기획이 불명확한 지점에서 AI가 임의로 확정하지 않고 개발을 멈추고 질문하도록 계약 — 실제로 카드 해금 방식(교체 vs 추가), 효과 중첩 규칙, 체력바 노출 조건, 튜토리얼 데이터 방식(SO vs JSON) 등 다수의 설계 결정이 이 질문 루프에서 확정되어 GDD에 역반영되었다.
3. **결정 로그 중심 개발**: 모든 결정을 "왜"와 함께 문서화하도록 해서, AI 산출물을 검수 가능하게 만들고 세션 간 일관성을 유지.
4. **AI 산출물 간 통합도 사람이 주도**: 서로 다른 AI가 만든 코드가 충돌한 사례(§4-3)를 겪으며, 복수 AI 협업에서는 "기존 시스템 확인 후 위임" 원칙이 필요함을 확인.

---

## 4. 어려웠던 점과 해결

### 4-1. `Time.timeScale = 0` 일시정지가 "새는" 버그 3연타

카드 선택/게임오버/튜토리얼 화면은 모두 `Time.timeScale = 0`으로 게임을 멈추는데, Unity에서 timeScale은 `deltaTime`만 0으로 만들 뿐 `Update()` 실행과 입력 폴링은 계속 돈다. 그 결과 (1) 일시정지 화면 뒤에서 타워 건설 클릭이 그대로 동작하고, (2) 플레이어가 멈춘 것처럼 보이면서도 방향 전환·스킬 발동은 되고, (3) 효과음 강제 컷오프 타이머가 일시정지 중 멈춰 원본 클립이 끝까지 재생되는, 겉보기에 무관해 보이는 버그 3개가 각각 발견되었다.

**해결**: 세 버그가 같은 근본 원인임을 파악한 뒤 일관된 원칙으로 통일 — 입력 폴링이 있는 모든 `Update()` 최상단에 `if (Time.timeScale <= 0f) return;` 게이트를 두고, 일시정지 중에도 흘러야 하는 타이머(효과음 컷오프, 게임오버 화면의 씬 전환 지연, 버튼 호버 애니메이션)는 전부 `Time.unscaledDeltaTime` 기반 수동 타이머로 교체했다. 개별 매니저 상태(`IsChoosing`/`IsGameOver`)를 하나씩 확인하는 대신 timeScale 하나만 보게 해서, 이후 튜토리얼 같은 새 일시정지 화면이 추가되어도 기존 코드를 수정할 필요가 없어졌다.

### 4-2. ScriptableObject 에셋 오염 — "플레이할 때마다 데이터가 망가지는 느낌"

업그레이드 카드가 `TowerDefinition.Data`의 수치를 직접 수정하고 해금 카드가 `TowerRoster` 리스트에 직접 추가하는 구조였는데, 이들은 프로젝트 에셋이라 Play 모드를 꺼도 변경이 원복되지 않는다 — 플레이를 반복할수록 원본 에셋 수치가 누적 오염되고 있었다.

**해결**: 세션 시작 시 원본 에셋을 `Instantiate()`로 복제하고 모든 시스템이 복제본만 참조하는 "런타임 클론" 패턴 도입. 복제본 캐시를 원본 쪽 `[NonSerialized]` 필드에 둬서 어떤 경로로 접근하든 세션 내 항상 같은 복제본을 받도록 보장했다. 이후 게임오버 Retry(`SceneManager.LoadScene`)가 에디터 재시작과 달리 static/캐시 상태를 초기화하지 않는다는 파생 문제도 발견 — 클론 캐시/이펙트 풀/전역 버프 레지스트리를 `GameManager.Awake()`에서 일괄 초기화하고, `[DefaultExecutionOrder(-1000)]`로 이 초기화가 씬의 어떤 스크립트보다 먼저 실행되도록 순서를 보장해 해결했다.

### 4-3. 서로 다른 AI가 만든 코드의 충돌 — 볼륨 슬라이더가 두 시스템으로 병렬 존재

설정 화면의 볼륨 슬라이더를 내려도 BGM이 작아지지 않는 문제가 발생했다. 원인은 Codex가 만든 타이틀 설정 화면 컨트롤러가 자체 볼륨 처리(자기만의 PlayerPrefs 키에 저장만 하고 실제 오디오엔 미반영)를 갖고 있었고, Claude Code가 만든 AudioMixer 기반 SoundManager와 완전히 분리된 반쪽짜리 병렬 시스템이었기 때문 — 같은 역할의 코드가 두 AI에 의해 따로 만들어져 "둘 다 있지만 실제로 쓰이는 쪽이 하필 미완성"인 상황이었다.

**해결**: 설정 컨트롤러의 볼륨 로직을 SoundManager로 완전히 위임하도록 재배선하고(슬라이더 → SetVolume 직결, Apply → 저장만), 죽은 코드가 된 중복 스크립트는 씬 참조가 없음을 확인 후 삭제. 복수 AI 협업에서는 새 UI를 붙이기 전에 기존 매니저/서비스의 존재를 확인시키는 절차가 필요하다는 교훈을 얻었다.

### 4-4. 적 회전 버그 — 증상은 하나, 원인은 두 개

일부 적은 회전이 아예 안 먹고 일부는 거꾸로 보이는 버그. 조사 결과 독립된 원인 두 개가 겹쳐 있었다: (1) 스프라이트 정면 보정값이 공용 프리팹에 하나뿐이라 아트마다 다른 기본 방향(위/아래)을 전부 못 맞췄고, (2) "이전 프레임 대비 위치 변화량"으로 방향을 추정하는 방식이 스폰 직후나 웨이포인트 스냅 프레임처럼 실이동량이 0에 가까운 순간 방향을 갱신하지 못했다.

**해결**: 보정값을 프리팹에서 종류별 데이터(`EnemyDefinition`)로 이동하고, 방향 계산을 이동 델타 대신 "현재 목표 웨이포인트를 향하는 벡터"로 교체 — 실이동 여부와 무관하게 항상 의도된 방향을 즉시 반영한다. 프로젝트 전반에서 반복된 "시각 표현 문제는 로직이 아니라 표현 레이어에서만 해결한다"는 원칙(이동 로직은 한 줄도 안 건드림)을 유지했다.

### 4-5. AI 생성 아트의 파이프라인 통합

AI로 생성한 스프라이트는 해상도가 제각각(224×305, 209×368 등)이라 매번 Pixels Per Unit을 수동 계산해야 했고, 크기를 맞추려 `transform.localScale`을 조정하면 같은 오브젝트의 사거리 판정용 콜라이더 반경까지 같이 커지는 부작용이 있었다. AI 생성 효과음도 뒤에 최대 2초의 불필요한 여백이 붙어 있었다.

**해결**: 스프라이트는 실제 bounds를 읽어 목표 크기에 맞는 스케일을 자동 계산하는 유틸리티를 만들고, 콜라이더 반경을 스케일만큼 역보정해 시각 크기와 판정 사거리를 분리(건설 프리뷰와 실제 건설이 같은 계산을 공유해 크기 불일치도 방지). 효과음은 클립 길이와 무관하게 0.5초에 강제 컷오프하는 재생 풀을 SoundManager에 내장했다. AI 산출물의 품질 편차를 에셋을 일일이 다듬는 대신 코드 레이어에서 흡수한 사례.

### 4-6. BGM 라이선스 리스크의 사전 차단

BGM 후보로 검토하던 NCS(NoCopyrightSounds)의 무료 라이선스가 게임 임베드까지 허용하는지 확신이 없어, AI에게 학습 데이터 기억이 아닌 **실시간 약관 페이지 조회**를 요청했다. 확인 결과 NCS 무료 라이선스는 영상/스트리밍 한정이고 게임 배포물 삽입은 별도 상업 라이선스 대상이었다 — 평가용 제출물에서 발생할 수 있었던 라이선스 문제를 사전에 차단하고, 게임 사용이 명시적으로 허용된 무료 소스(Pixabay Music)로 교체했다.

### 4-7. 입력 이중 발화 — 타워는 지어지는데 "슬롯 부족" 대사가 같이 뜸

타워가 정상 설치되는데도 매번 건설 실패 대사가 함께 출력되는 기묘한 증상. 원인은 Input System 액션이 마우스를 누를 때와 뗄 때 모두 Performed를 발생시키는 상호작용으로 설정돼 있어, 클릭 한 번에 건설 로직이 두 번 실행된 것(누를 때 성공 → 뗄 때 같은 셀이 이미 점유라 실패 이벤트 발생).

**해결**: `Mouse.current.leftButton.wasPressedThisFrame` 직접 폴링으로 교체해 물리적으로 누르는 순간에만 정확히 1회 반응하도록 수정. 이후 스킬 입력이 같은 액션(좌클릭 포함)에 물려 건설 클릭과 겹치는 문제도 연달아 발견되어 스킬을 스페이스바 직접 폴링으로 분리 — 프로젝트의 입력 처리 스타일을 직접 폴링으로 통일하는 계기가 되었다.

---

## 부록

- **설계 문서**: Notion — R&C Company > GDD 초안 (기획 의도, 시스템 명세, 기술 노트, 효과 중첩 규칙, 오퍼레이터 대사 스크립트)
- **개발 로그**: `Assets/Docs/ARCHITECTURE.md` (구조적 결정 전체 기록, 약 1,100줄) / Notion — 구현 체크리스트 (Day 1-3)
- **AI 프롬프트 기록**: Notion — R&C Company > 제작과정 (초기 협업 규칙 프롬프트, NovelAI 오퍼레이터 프롬프트 전문 등)
